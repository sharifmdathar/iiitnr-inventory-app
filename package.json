{
  "name": "iiitnr-inventory-app",
  "private": true,
  "workspaces": ["backend"],
  "scripts": {
    "lint": "bun run --cwd backend lint",
    "lint:fix": "bun run --cwd backend lint:fix",
    "typecheck": "bun run --cwd backend typecheck",
    "format": "bun run --cwd backend format && cd app && ./gradlew ktlintFormat",
    "fmt": "bun run format",
    "dev": "bun run --cwd backend dev"
  }
}
